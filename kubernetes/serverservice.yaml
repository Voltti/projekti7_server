apiVersion: v1
kind: Service
metadata:
  name: ryhma4-server
  namespace: default
  labels:
    app: ryhma4-server
spec:
  selector:
    app: ryhma4-server
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
